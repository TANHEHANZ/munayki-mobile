import { View, Text } from "react-native";
import React from "react";
import { colors } from "../styles/CompStyle";

const Navigation = () => {
  return (
    <View style={{ height: 80, backgroundColor: colors.CC }}>
      <Text>Navigation</Text>
    </View>
  );
};

export default Navigation;
